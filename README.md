# Pizzeto — A fast food restaurant web page

A full stack web application for a fast food restaurant. 

Tech Stack:

- React
- Node.js
- Express
- MongoDB

# Setup

- Clone the repository `git clone https://github.com/mernbit/Pizzeto`
- Install dependencies `npm install` or `npm i`
- Set up environment variables in frontend and backend both
- Run the development server `npm run dev`

